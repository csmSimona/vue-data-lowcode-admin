// 直角坐标系通用配置
export const commonOption = {
  // 轴是否展示
  xAxisShow: true,
  yAxisShow: true,
  // 轴线是否展示
  xLineShow: true,
  yLineShow: true,
  // 网格线是否展示
  xGridLineShow: false,
  yGridLineShow: false,
  // 坐标轴刻度线是否展示
  xTickShow: true,
  yTickShow: true,
  // 坐标轴刻度标签是否展示
  xLabelShow: true,
  yLabelShow: true,
  // 坐标轴刻度标签颜色
  xLabelColor: '#fff',
  yLabelColor: '#eee',
  // 坐标轴线颜色
  xLineColor: '#fff',
  yLineColor: '#eee',
  // 距离
  grid: {
    left: 10,
    top: 20,
    right: 10,
    bottom: 10,
  }
};
